"""
                            Arrays

* collection of items stored at contiguous memory locations
* idea is to store multiple items of the same type together
* in python array is handled by a module called array.
* array can be useful when we have to manipulate only specific data type values
* all elements of array must be the same

"""